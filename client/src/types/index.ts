export interface StripType {
  id: string;
  name: string;
  panelCount: number;
}

export interface Filter {
  id: string;
  name: string;
  preview: string;
}

export interface Frame {
  id: string;
  name: string;
  preview: string;
}
